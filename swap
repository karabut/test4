void Swap(int *first, int *second)
{
    if(*first != *second)
    {
        T tmp = *first;
        *first = *second;
        *second = tmp;
    }
}


void swap(int &first ,int &second)
{
    int tmp = first;
    first = second;
    second = tmp;
}
