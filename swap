void Swap(int *first, int *second)
{
    if(*first != *second)
    {
        T tmp = *first;
        *first = *second;
        *second = tmp;
    }
}


void swap(int &first ,int &second)
{
    first = first + second;
    second = first - second;
    first = first - second;
}
